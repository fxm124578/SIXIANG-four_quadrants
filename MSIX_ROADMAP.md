# MSIX 长期发布计划

> 状态：**长期暂缓，不进入当前开发**  
> 最后确认：2026-09-03  
> 当前发布通道：便携版 EXE + GitHub Releases（`main`，当前版本 `v1.3.23`）

## 决策记录

- 当前产品只发布免费版；不做付费、试用、订阅、内购或许可证校验。
- 当前不采用 MSIX、不注册 Microsoft Store 开发者账号、不调整 Git 分支策略。
- 保留本计划，是为了未来需要更标准的安装、卸载、商店更新或企业分发时，不必重新从零梳理风险。
- 在重新批准前，`main` 继续承载便携版，不能把它视为 MSIX 稳定版分支；`portable` 分支也不创建。

## 何时重新启动

满足以下任一条件时，可由产品负责人明确指示“启动 MSIX 路线”：

1. 用户频繁反馈下载、解压、替换更新或 Windows SmartScreen 的问题。
2. 产品需要 Microsoft Store 的搜索、安装、自动更新或企业分发能力。
3. 需要正式的安装目录、开始菜单入口和系统卸载入口。

重新启动前应先决定分发方式：

| 选择 | 适用情况 | 额外前置条件 |
|---|---|---|
| Microsoft Store（推荐） | 面向普通用户的免费版 | 注册个人 Partner Center 账号、预留应用标识、完成商店资料 |
| 直链 MSIX / App Installer | 内测或私有分发 | 代码签名证书、托管更新清单与下载地址 |

免费版优先选择 Microsoft Store。商店负责签名、托管和更新；直链 MSIX 的证书和信任成本不适合当前阶段。

## 目标与边界

未来 MSIX 版要解决的是“安装和更新更可靠”，不是重写产品：

- 采用 PyInstaller `onedir` 作为 MSIX 内部内容，不再运行 onefile 的临时解压目录，降低 `python3xx.dll` 相关启动风险。
- 由 Store 负责下载、替换和更新检查；MSIX 版移除 GitHub 自更新入口。便携版仍保留现有 GitHub Releases 更新器。
- 任务数据、导出、备份和用户安装的主题放到用户可见的 `Documents\四象\`；不能放在 MSIX 包目录或 `LocalState`。
- 卸载时不依赖 Windows 弹出“数据将被删除”的提示。数据库和自定义主题在 `Documents\四象\`，因此卸载应用后仍保留；应用内明确展示数据位置并提供备份入口。
- 不做付费、试用、商店许可证桥接或账号登录。

## 分阶段实施计划

### 阶段 A：数据根目录与迁移（1–2 人日）

**结果**：不论 EXE 或 MSIX，用户任务数据都有明确且可备份的位置。

- 新增统一的数据根目录规则：`Documents\四象\`。
- 首次启动 MSIX 时发现旧 EXE 目录的 `data.db`、主题和设置后，复制到新位置；原文件保留，迁移结果可见。
- 设置页展示数据路径，提供“打开数据目录”和 SQLite 数据库备份。
- 为失败、重复迁移、只读目录、同名冲突设计可恢复提示。

**验收**：从旧便携版迁移后任务、日报、设置和主题完整；卸载 MSIX 后上述数据仍存在。

### 阶段 B：MSIX 打包与运行时（2–3 人日）

**结果**：可安装、启动、卸载的本地 MSIX 候选包。

- 以 `onedir` 打包，确认 Python DLL、WebView、tkinter 回退和内置主题均随包交付。
- 生成 MSIX Manifest，声明应用标识、图标、最小 Windows 版本和启动入口。
- 内置资源保持只读；用户主题只从数据根目录加载。
- 自启动改用 MSIX 支持的 StartupTask/清单能力，不能继续依赖 HKCU `Run` 写入。
- MSIX 版隐藏 GitHub“检查更新 / 重启更新”；便携版保持现有机制。

**验收**：全新 Windows 用户下安装、运行、退出、开机自启和卸载均可完成；不出现 `python314.dll` 加载错误。

### 阶段 C：主题库与远端主题（2–3 人日，可独立延期）

**结果**：用户可预览、选择并按需下载官方主题，而非手动找 CSS。

- 为主题建立独立的官方发布源，不能混入应用更新 Release；使用 `themes-index.json` 提供主题 ID、名称、说明、版本、预览图、CSS 资产、最低应用版本、大小与 SHA-256。
- 设置页新增“主题库”：内置 / 已安装 / 可下载三类卡片，先显示缓存预览图，用户点选后才下载 CSS。
- 下载前校验主题文件名、体积、SHA-256 和 CSS 结构；下载后放入 `Documents\四象\themes\`。
- 离线时仍可使用内置和已安装主题，并明确提示“暂时无法刷新主题库”。
- 不将未经校验的远端 CSS 或图片 URL 直接注入页面。

**验收**：可浏览预览、安装、切换、更新、移除主题；网络中断或损坏主题不会影响应用启动。

### 阶段 D：Store 上架与更新验证（2–3 人日）

**结果**：免费 MSIX 稳定版可在 Microsoft Store 安装与更新。

- 注册个人 Partner Center 账号、预留“**四象**”名称和获取包标识；该动作只在本计划重新批准后执行。
- 准备商店页中文文案、截图、图标、隐私说明、支持邮箱与免费定价。
- 使用 Store Flight 做内测：验证由旧 Store 包升级、启动、数据迁移和回退。
- 正式发布后验证 Store 自动更新，确保不再由应用自行下载或覆盖安装目录。

**验收**：从商店安装、更新与卸载全流程通过；用户数据不丢失；便携版和 MSIX 可共存且数据迁移规则明确。

## 成本与依赖

| 项目 | 估计投入 | 说明 |
|---|---:|---|
| 数据迁移与备份 | 1–2 人日 | 高风险，必须先做 |
| MSIX/onedir 打包与自启动 | 2–3 人日 | 包括真实 Windows 环境验证 |
| 远端主题库 | 2–3 人日 | 可放到 MSIX 首发后 |
| 商店资料、Flight 与发布验证 | 2–3 人日 | 含审核往返缓冲，不等于审核时长 |
| **合计** | **7–11 人日** | 不包含付费/试用，因为已明确不做 |

外部依赖：Microsoft Store 路线需个人 Partner Center 账号和应用名称；直链路线另需受信任代码签名证书。当前免费策略不需要许可证服务。

## 重新启动时的首批工作

只做阶段 A，先交付“数据迁移 + 备份 + 卸载后保留数据”的可验证结果；未通过验收前，不开始 MSIX 打包、主题库或商店上架。

## 当前不做

- 不创建 MSIX 分支、不改 `main` 的发布职责。
- 不申请 Microsoft Store 账号或名称。
- 不迁移现有用户数据。
- 不增加付费、试用、订阅、内购、授权码或登录系统。
- 不修改当前便携版 `v1.3.23` 的 GitHub Releases 更新方案。
